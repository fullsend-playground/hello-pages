// Entry point for shared client-side behavior across pages.
// Add site-wide scripts here as the project grows.

if (import.meta.env.DEV) {
  console.info('hello-pages dev server running with live reload');
}
